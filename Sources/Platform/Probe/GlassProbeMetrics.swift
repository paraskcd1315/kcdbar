import CoreGraphics
import Foundation

enum GlassProbeMetrics {
    static let surfaceSize = CGSize(width: 320, height: 140)
    static let gap: CGFloat = 24
    static let windowTitle = String(localized: "probe.window.title")
}
