import CoreGraphics

/** The size of a readout sitting in the bar's tray side. */
package enum TrayItemMetrics {
    package static let controlCentreWidth: CGFloat = 24
    package static let glyphSymbol = "switch.2"
}
