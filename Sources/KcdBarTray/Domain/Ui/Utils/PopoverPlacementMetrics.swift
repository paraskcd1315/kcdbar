import CoreGraphics

/** How far a popover sits from the item that opened it. */
package enum PopoverPlacementMetrics {
    package static let gap: CGFloat = 6
    package static let centringDivisor: CGFloat = 2
    package static let sizeTolerance: CGFloat = 1
}
