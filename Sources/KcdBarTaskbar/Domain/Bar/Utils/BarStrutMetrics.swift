import CoreGraphics

package enum BarStrutMetrics {
    /** The height Tahoe's Dock reserves in every app's visible frame, whatever its tile size. */
    package static let dockStrutHeight: CGFloat = 48
}
