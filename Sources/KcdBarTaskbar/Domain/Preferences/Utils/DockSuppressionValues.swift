/** A hidden Dock that never reveals on hover, and Paras's own minimize effect. */
package enum DockSuppressionValues {
    package static let autohide = true
    package static let autohideDelay: Double = 1000
    package static let autohideTimeModifier: Double = 0
    package static let minimizeEffect = "scale"
}
